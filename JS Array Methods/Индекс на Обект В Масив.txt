let people = [
    {name: 'Krasen' , age:32 , sex: 'male'},
    {name: 'Donika' , age:28 , sex: 'female'}
]

let objIndex = people.findIndex((obj => obj.name === 'Krasen'));
  // Намира индекса в масива за да можем да го ползваме за нещо

  // Може да търсим и по другите ключове 
  // Например:
let objIndex = people.findIndex((obj => obj.age === 32));