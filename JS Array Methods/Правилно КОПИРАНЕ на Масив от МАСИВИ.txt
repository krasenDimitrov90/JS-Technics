let arr = [
    [3,2,1],
    [1,3,2],
    [1,2,3]
]
let copyArr = arr.map(el => el.slice(0))

Т.к. аrr  е масив от масиви ако директно напишем let copyArr = arr.slice(0)
няма да се копира правилно и в последствие ако променим нещо по който и да е 
от двата масива ще се отрази и на другия.
Затова първо ги мапваме и върху всеки един вътрешен масив прилагаме метода slice(0);